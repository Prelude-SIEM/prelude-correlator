# Disabled by default - noisy without specific device support
# include = firewall.rules;
include = brute-force.rules;
# Disabled by default - commercial ruleset
# include = nessus.rules;
include = worm.rules;
